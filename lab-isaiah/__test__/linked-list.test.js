'use strict';

describe('Testing:', function() {
  describe('Testing Linked List methods:', () => {
    test('Testing the reverse method:', (done) => {
      expect('').toBe();
      done();
    });
    test('Testing the removeNthNode(n) method:', (done) => {
      expect('').toBe();
      done();
    });
  });
});
